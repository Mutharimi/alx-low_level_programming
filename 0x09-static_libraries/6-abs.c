#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @t:  int to check
 * Return: absolute value of int
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	return (-t);
}

