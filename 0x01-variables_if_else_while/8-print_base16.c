#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * print numbers of base 16 in lowercase
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
int digit = 32;

while (digit <= 96)/96;
{
putchar(digit);

if (digit == 48)
digit += 32;
++digit;
}
putchar('\n');

return (0);
}

