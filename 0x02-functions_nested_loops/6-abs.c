#include "main.h"
/**
 * _abs - computes the absolute value of integer
 * @t: The int to check
 * Return: the absolute value of int
*/

int _abs(int)
{
	if (n > 0)
{	
	return (n);

} 
	return (-t);
}
